NODE_ENV=production
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aaral_bridge
DB_USER=postgres
DB_PASSWORD=__SET_TO_THE_POSTGRES_PASSWORD_YOU_SET_DURING_INSTALL__
DB_SSL=false
WA_SESSION_PATH=./wa-sessions
PROOFS_PATH=./proofs
CHROME_PATH=

# Left empty on purpose — this is what makes the bot go live for real
# customers instead of only the test allowlist. Do not copy the dev file's
# TEST_MODE_ALLOWED_NUMBERS value here.
TEST_MODE_ALLOWED_NUMBERS=
